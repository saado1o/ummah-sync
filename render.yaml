services:
  - type: web
    name: ummahsync-ai-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      # Note: Set GEMINI_API_KEY in the Render dashboard manually for security
